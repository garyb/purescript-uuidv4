{
  "private": true,
  "scripts": {
    "build": "purs-tidy check --config-require src/**/*.purs && pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^16.0.2",
    "purescript-psa": "^0.8.2",
    "purs-tidy": "^0.9.2"
  }
}
